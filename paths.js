const ROOT = __dirname;

const paths = {
  root: ROOT,
  entry: ROOT + '/src/routes.js',
  src: ROOT + '/src',
  build: ROOT + '/build',
  bower: ROOT + '/bower',
  node_modules: ROOT + '/node_modules',
};

export default paths;
